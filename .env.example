# Copy to ".env" and fill values as needed.

# Optional for Phase 0; required once you integrate an LLM.
LLM_PROVIDER=openai
LLM_API_KEY=

# Groq (Phase 4)
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Dataset config (used in Phase 1+)
HF_DATASET_ID=ManikaSaini/zomato-restaurant-recommendation
HF_DATASET_SPLIT=train

# App behavior
LOG_LEVEL=INFO

# Phase 7 API — comma-separated browser origins (localhost defaults for dev)
API_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001
# Production on Render: also add your Vercel URL, e.g. https://your-app.vercel.app
# Render auto-allows *.vercel.app via regex when API_CORS_ALLOW_VERCEL=1 (default)
# API_CORS_ALLOW_VERCEL=1
# API_CORS_DISABLE_LOCALHOST_REGEX=1

# Production dataset path (Render persistent disk at /var/data)
# ZOMATO_PROCESSED_DATASET=/var/data/restaurants.parquet

# Render: skip build-time ingest when Parquet lives on disk (see render.disk.yaml)
# RENDER_SKIP_DATASET_INGEST=1

# Run Phase 1 ingest on API startup if Parquet is missing (first deploy ~1–4 min)
# ZOMATO_AUTO_INGEST_IF_MISSING=0


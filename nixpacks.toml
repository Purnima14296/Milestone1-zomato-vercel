# Nixpacks (Railway) — always use `python -m pip` (pip may not be on PATH).

[variables]
NIXPACKS_PYTHON_VERSION = "3.12"

[phases.install]
cmds = [
  "python3 -m ensurepip --upgrade",
  "python3 -m pip install --upgrade pip setuptools wheel",
  "python3 -m pip install \".[api]\"",
]

# Nixpacks (Railway) — https://nixpacks.com/docs/configuration/file
# Complements railway.toml; Python version also set in runtime.txt

[phases.setup]
nixPkgs = ["python312", "bash"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[variables]
NIXPACKS_PYTHON_VERSION = "3.12"
